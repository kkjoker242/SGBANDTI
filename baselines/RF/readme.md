项目要求：
1、请你查看项目中dataset文件夹下的俩个数据集，要求在训练集上训练，保存最优的val（验证集）的最优模型，并最后用测试集进行测试模型（返回6个指标：AUC、AUPRC、F1、Sensitivity、Specificity、Accuracy（6个指标））
2、以RF库封装好的算法，跑DTI（药物靶标相互作用预测）方向的实验，用来与其他模型做对比。
3、保存存AUROC和AUPRC两个曲线所需要的数据文件（比如npy）
